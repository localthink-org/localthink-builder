{
  "name": "localthink-builder",
  "version": "0.3.1",
  "description": "Chrome extension for exporting ChatGPT, Claude, Gemini, and Grok conversations as LocalThink Format files.",
  "type": "module",
  "license": "MIT",
  "private": true,
  "scripts": {
    "check": "node --check popup.js && node --check src/content.js && node --check src/ltf.js",
    "smoke": "node scripts/smoke.mjs"
  }
}
